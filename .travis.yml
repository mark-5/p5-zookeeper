language: perl
perl:
  - "5.24"
  - "5.22"
  - "5.20"
  - "5.18"
  - "5.16"
  - "5.14"
  - "5.12"
  - "5.10" 

before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install libzookeeper-mt-dev

install:
  - cpanm --with-configure --with-develop --installdeps --notest .
